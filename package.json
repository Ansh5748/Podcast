{
  "name": "podcast-studio-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend --legacy-peer-deps && cd backend && python -m venv venv && .\\venv\\Scripts\\python.exe -m pip install -r requirements.txt",
    "start:frontend": "npm start --prefix frontend",
    "start:backend": "cd backend && .\\venv\\Scripts\\python.exe -m uvicorn server:app --reload --port 8000",
    "dev": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "install:backend": "cd backend && python -m venv venv && .\\venv\\Scripts\\python.exe -m pip install -r requirements.txt",
    "install:frontend": "npm install --prefix frontend --legacy-peer-deps"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
